# Роли агентов Community Bot

Роли описывают инженерный процесс Jira `CB`, а не будущие исполняемые роли самого Telegram-бота.

Исполняемые модели и бюджеты определяет только глобальная политика
`codex.agent-budget.v1`. Проектная карта ролей, условная загрузка документов и
compact packets определены в `community_bot.orchestration.v2` внутри
`agents/config.yaml`; правила применения описаны в
`docs/AGENT_CONTEXT_AND_COST_POLICY.md`. Числовые execution limits в инструкциях
ролей не дублируются.

## Каталог

### `developer`

Основная роль по умолчанию. Ведёт Jira-задачу через приём, проверку готовности, планирование, ограниченную реализацию, тестирование, отчёт и передачу результата. Для малых воспроизводимых багфиксов уровня `1B` по ADR-0010 использует compact bugfix note вместо полного плана и отчёта, но сохраняет Jira-задачу и ветку `task/<ISSUE-KEY>`. Для явно запрошенных багов, функций и изменений процесса действует постоянное намерение владельца на создание Jira-задачи. Каждая задачная ветка после успешных gates должна дойти до push, PR и merge; остальные внешние изменения требуют отдельного ясного намерения пользователя.

### `analyst-architect`

Опциональная роль. Активируется по прямому запросу для анализа требований, архитектурного решения, ADR или проверки консистентности. Не реализует код и не выполняет внешние изменения.

### `plan-reviewer`

Независимая роль только для чтения и смысловой проверки `plan.md` и `plan-source-context.md`. Обязательна для задач уровня 3, включая архитектурные, интеграционные, чувствительные к безопасности и насыщенные источниками; для малых задач запускается по необходимости.

### `final-review`

Обязательная независимая роль после `implementation-report.md` для уровней 2–3. Для уровня `1B` выполняет короткую diff-проверку compact bugfix note, фактической разницы, целевой проверки, секретов и признаков повышенного риска; локальный `final-review.md` создаётся только при явной необходимости. Не исправляет результат и не выполняет внешние изменения.

## Будущие исполняемые роли

Для будущего каталога рассматриваются общие архетипы:

- оркестрация;
- приём и нормализация;
- интерпретация и классификация;
- агрегация и сверка;
- предварительный просмотр и проверка человеком;
- детерминированное применение и завершение.

Фактический каталог исполняемых агентов Community Bot появится только после утверждения продуктовых сценариев.

## Структура

```text
agents/
  README.md
  config.yaml
  workflow.yaml
  developer/
    README.md
    instruction.md
    procedures.md
    config.yaml
    templates/
  analyst-architect/
    README.md
    instruction.md
    procedures.md
    config.yaml
  plan-reviewer/
    README.md
    instruction.md
    config.yaml
    templates/
      plan-review.md
  final-review/
    README.md
    instruction.md
    procedures.md
    config.yaml
    templates/
```

## Общий контракт

Каждая роль содержит:

- стабильные идентификаторы правил;
- декларацию идемпотентности;
- закрытый список сценариев ошибок;
- версионированную схему результата;
- самопроверяемые критерии завершения.

При многопоточной работе роль получает один ограниченный deliverable и
релевантные ссылки через task/review packet. Полную историю оркестратора и все
канонические документы по умолчанию не передавать. Большой вывод сохранять в
файл и возвращать компактное доказательство по `tool_output` из
`agents/config.yaml`.

Вердикты артефактов ревью всегда используют одно из точных значений:

```text
Status: approved
Status: changes_requested
Status: blocked
```

Все смысловые объяснения пишутся на русском. Технические идентификаторы сохраняются как есть.
