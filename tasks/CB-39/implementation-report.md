# CB-39 — отчёт реализации

## Статус

Реализовано локально в ветке `task/CB-39`. Jira-задача `CB-39` создана и
переведена в `В работе`.

Ветка `task/CB-39` пересобрана от актуального `origin/main`
(`c605b566a5f5e1fb799224bf4eb406f67fd05449`), чтобы новые задачи и доработки
почковались от текущего `main`, а не от старой смешанной истории. Старый указатель
с тремя несвязанными runtime/ops-коммитами сохранён локально как
`archive/CB-39-mixed-history-20260816`; незакоммиченные правки сохранены в stash
`CB-39 dirty process changes before rebuilding from main`. В новую ветку
перенесён только процессный diff `CB-39`; старые артефакты `tasks/CB-42` из stash
не переносились.

## Что изменено

- В канонических правилах закреплено: баги, функции, пользовательские истории,
  изменения runtime, workflow и процесса разработки требуют Jira-задачу до
  реализации.
- В Git-правилах закреплено имя ветки `task/<ISSUE-KEY>` для таких изменений.
- Уровень `1B` оставлен быстрым по артефактам, но больше не освобождает от Jira
  и ветки задачи.
- Добавлено постоянное правило: задачная ветка проходит commit, push, PR,
  CI/review и merge в `main`, если gate не заблокирован.
- Уровень 1 без Jira сужен до неповеденческих документационных правок.
- В workflow добавлена запись существенных событий в Jira без поштучного
  комментирования локальных команд.

## Критерии приёмки

- Jira-first правило для багов, функций и изменений поведения зафиксировано:
  выполнено.
- `1B` не отменяет Jira-задачу и ветку `task/<ISSUE-KEY>`: выполнено.
- Уровень 1 без Jira ограничен неповеденческой документацией: выполнено.
- `agents/workflow.yaml` и инструкции агентов обновлены: выполнено.
- Текущая ветка переименована в `task/CB-39`: выполнено.
- Маршрут задачной ветки до PR и merge при успешных gates закреплён: выполнено.

## Проверки

- `git diff --check`: прошло.
- YAML parse: прошло для `agents/config.yaml`, `agents/workflow.yaml`,
  `agents/developer/config.yaml`, `agents/final-review/config.yaml`,
  `config/testing/tg-test.yaml`.
- Поиск старых формулировок, где fast lane мог идти без Jira: противоречий для
  уровня `1B` не найдено; разрешённые исключения остались только для
  неповеденческого уровня 1.
- Проверка ветки: `task/CB-39` теперь стоит на `origin/main`, `git cherry -v
  origin/main task/CB-39` не показывает старых уникальных коммитов.

## Риски

Независимое `final-review.md` ещё не выполнено. До закрытия review/CI gate PR,
push, merge и финальный переход Jira ещё не выполнялись.
