# CB-61 — первая попытка ревью плана

Status: changes_requested

## Проверенная область

Проверены Jira-снимок, `plan-source-context.md`, `plan.md`, предложенный
ADR-0015, ADR-0003/0004/0007, текущие agent config/workflow и отсутствие
пересечения файлов с активными CB-49/50/58/59/60.

## Обязательные исправления

1. Зафиксировать точную версионированную YAML-схему и оставить исполняемые
   числовые лимиты только в `agents/config.yaml`.
2. Описать конечный автомат continuation/handoff с progress evidence,
   владельцем решения и терминальными состояниями.
3. Сопоставить критерии Jira с позитивными и негативными автоматическими
   проверками и точными командами без конфликта с coverage addopts.
4. Добавить pre-merge coordination gate для candidate-bound freeze CB-50.

## Итог

План возвращён на одно полное исправление пакета. Реализация не разрешена.

