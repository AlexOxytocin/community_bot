# CB-46 — final review

## Результат проверки

Блокирующих дефектов в diff не найдено.

## Проверенные риски

- Non-active участники не попадают в каталог, поиск и cursor-пагинацию.
- Поиск не использует скрытые Telegram `first_name`/`last_name`; доступны только публичные `telegram_username` и `display_name`.
- Callback-и каталога не несут поисковую строку и технически короткие: cursor передаётся как UUID последней строки, раскрытие — как номер строки.
- Старые действия `member:profile`, `karma:begin`, `karma:raw`, `mod:*`, `member:role:*` сохранены и вызываются из раскрытой строки.
- Production dispatcher smoke проходит после передачи `foundation` в reputation router.
- Старый output-driven сценарий обновлён под новый пользовательский путь: открыть каталог → раскрыть строку `+` → выбрать действие.

## Остаточные риски

- Производственная Telegram-проверка не выполнялась, поэтому статус — локально готово к PR, не production accepted.
- Поиск на 500+ участников ожидаемо нормальный без отдельного индекса, но при существенном росте базы стоит добавить индекс/триграммы отдельной задачей.
- В callback состояния используется номер строки текущей страницы. Если пользователь нажмёт очень старое сообщение после изменения состава каталога, сервер заново применит policy и перерисует страницу без раскрытия скрытых профилей; это безопасно, но может выглядеть как устаревший экран.

## Проверки

- `uv run ruff check .`
- `uv run ruff format --check .`
- `uv run ty check src tests ops\verify_release_provenance.py`
- `git diff --check`
- `uv run pytest -q` → `495 passed, 1 skipped`, coverage `80.38%`
