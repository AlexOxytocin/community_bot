CB-107 migration verification, 2026-08-20

uv run alembic heads
0022 (head)

uv run pytest tests/integration/test_web_api.py -q --no-cov -k profile_links_migration
1 passed, 16 deselected in 10.25s

The test executes 0021 -> 0022, validates the JSONB schema/default/checks,
then executes downgrade and upgrade again.
